import com.hzzz.www.queue.ArrayQueue;

public class ArrayQueueTest {
    public void test() {
        ArrayQueue<Integer> queue = new ArrayQueue<>();
        System.out.println("size: " + queue.size() + ", data length: " + queue.);
    }
}
